
class_list = ["Yosuf","Luis", "Rafiki", "Sam", "Saad", "Maddox","Chris"]

def get_earliest(my_list):
    #returns the index for the alphabetically lowest item
    earliest = 0
    #traverse list and compare each item to earliest

    return earliest

def selection_sort(my_list):
    # base case, coded as an if-statement. Refer to notes from class discussion!

    #recursive step, coded as else statement. get_earliest() will be useful here!:

    return my_list

print("Unsorted list:" + str(class_list))
print("Selection sort:" + str(selection_sort(class_list)))
